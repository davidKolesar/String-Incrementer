function incrementString (string) 
{
	//check if parameter is null
	if(string === null) 
	{
	  return 1;
	}

	//check if parameters contain numbers
	var chars = string.split('');
	var containsNumber = false;
	var numbersWithinString[];
	var numbersToReturn[];
	var totalDigitsOfNumber++;

	for(var i = 0; i < chars.length; i++) 
	{
	  if(typeof chars[i] == 'number')
	  {
		containsNumber = true;
		totalDigitsOfNumber++;
		numbersWithinString.push(chars[i]);
		break;
	  } 
	}
	  if(containsNumber === false)
	  {
		return string + 1;
	  }
	 
	 //add numbers
	 var finalNumber = numbersWithinString[numbersWithinString.length];
	 
	 if(finalNumber === 9)
	 { 
	 
	 } 
	 else
	 {
	  var numbersWithinString[totalDigitsOfNumber] = 
	  numbersWithinString.splice(index, 0, objectName)
	  var newSum = [numbersWithinString].join("");
	 }
	 
}