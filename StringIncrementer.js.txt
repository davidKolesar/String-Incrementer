function incrementString (argument) 
{

//check if parameter is null
if(argument === null) 
{
  return 1;
}

//check if parameters contain numbers
var chars = argument.split('');
var containsNumber = false;
var numbersWithinString = [];
var numbersToReturn = [];
var lettersWithinString = [];


for(var i = 0; i < chars.length; i++) 
{
  if(typeof chars[i] == 'number')
  {
    containsNumber = true;
    numbersWithinString.push(chars[i]);
    break;
  } 
  else 
  {
  lettersWithinString.push(chars[i]);
  }
}
  if(containsNumber === false)
  {
    return argument + 1;
  }
 
//add numbers
var argumentNumberWithCommas = numbersWithinString.toString();
var argumentNumberWithoutCommas = argumentNumberWithCommas.replace(",", "");
var argumentNumber = argumentNumberWithoutCommas.parseInt();
var finalNumber = argumentNumber++;
 
 return argument + finalNumber;
 
}